function setup() {
    createCanvas(window.innerWidth, window.innerHeight);
} 
function draw() {
p = new Particle();
}
function draw() {
    p.draw()
}

class Particle {
    constructor() {
        // Position
        this.pos = createVector(random(width), random(height));
        // Size
        this.size = 10;
    }
    
}